blueprint:
  name: Door-activated notification
  description: Sends a notification when a door sensor is activated.
  domain: automation
  source_url: https://github.com/dioalmeida/blueprints/new/main/door_sensor.yaml
  input:
    door_entity:
      name: Door magnet open/closed Sensor
      selector:
        entity:
          domain: binary_sensor
    #      device_class: opening
    #light_target:
    #  name: Light
    # selector:
    #    target:
    #      entity:
    #        domain: light

# If motion is detected within the delay,
# we restart the script.
#mode: restart
#max_exceeded: silent

#trigger:
#  - platform: state
#    entity_id: !input door_entity
#    to: "on"
#  - platform: state
#    entity_id: !input door_entity
#    to: "off"
#
#action:
#  - service: light.turn_{{trigger.to_state.state | lower}}
#    target: !input light_target
